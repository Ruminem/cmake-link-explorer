# NEXT

**여기까지 됨** — `c++filt` 문제를 해결했다. 윈도우에는 `c++filt`가 없는데(Git for
Windows도 번들하지 않는다) 맵은 리눅스 빌드에서 나오므로 심볼이 계속 맹글링된 채로
보였다. `src/demangle.js`를 새로 만들어 외부 도구가 없거나 이름을 그대로 돌려줄 때
받아내게 했다.

**읽을 가치가 있는 형태만** 다룬다 — 네임스페이스 함수, 멤버 함수, 생성자·소멸자,
연산자, 기본 타입, 포인터·레퍼런스, 백레퍼런스, `typeinfo`/`vtable`. 템플릿과 ABI
태그는 거절하고 맹글링된 이름을 그대로 둔다. 커밋된 맵의 심볼 503개로 확인:
**사용자 코드 21개 전부 해석, libc++ 노이즈 476개는 하나도 건드리지 않음.**
그 476개는 풀어도 200자짜리 템플릿이라 읽을 가치가 없다.

전체 **100개 통과, 실패 0** (`map-test` 49 → 62). 기존 테스트
"demangling survives a missing demangler"는 "디맹글러가 없으면 아무것도 안 풀린다"를
단언하고 있었는데 이번 변경이 그 전제를 뒤집으므로, **추측하지 않는다**를 검사하도록
바꿨다(템플릿 심볼이 하나라도 손대졌으면 실패).

**다음 할 것** — README의 macOS 전용 명령을 윈도우와 병기한다. 지금까지 실제로 걸린
것만 네 군데다: 설치의 `ln -s` → `mklink /J`, 통합테스트의 `/Applications/...` 경로,
`/tmp/it.log`, 그리고 `python3` → `python`(윈도우에서는 스토어 스텁에 걸린다).
`test/bootstrap.sh`도 sh 스크립트라 윈도우에서는 `python test/make-fixture.py`를
직접 부르는 편이 낫다는 안내가 필요하다.

**보류** — MSVC 맵 포맷(`link.exe /MAP`). 파서 신규 작성은 주말 하나를 통째로 먹는데,
**제품 바이너리는 리눅스 GNU 빌드라 이미 지원된다.** 윈도우 빌드의 용량을 볼 일이
실제로 생기면 그때 하고, 그때도 실물 샘플부터 확보한다(추측 금지 원칙).

**막힌 것** — 확장 호스트 35개는 VS Code 실행 경로가 macOS 전용이라 미실행.
`include-test`는 28개가 실제 빌드 트리를 요구한다(합성 픽스처로 풀리는지 미검증).
윈도우에 C++ 컴파일러가 없어 진짜 빌드는 불가.
