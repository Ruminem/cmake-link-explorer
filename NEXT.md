# NEXT

**여기까지 됨** — 코드 점검에서 잔 버그 3건을 찾아 고쳤다. 전체 **105개 통과, 실패 0**
(`map-test` 63, `include-test` 2 → 6).

1. **`cmakeEdit`이 CRLF 파일에 LF를 꽂았다.** 윈도우 `CMakeLists.txt`는 보통 CRLF인데
   Quick Fix가 항상 `'\n'`을 넣어, 편집한 줄 하나만 개행이 달라졌다. diff와 개행 린트에
   그대로 걸린다. 윈도우 대상 확장에서 제일 실질적인 버그였다. 파일이 이미 쓰던 개행을
   따라가게 고쳤고, append/create × CRLF/LF × 한 줄/여러 줄 경로를 테스트로 덮었다.
2. **`formatBytes`의 단위 사다리가 MB에서 끊겼다.** 1 GB가 `1024.00 MB`로 나왔다.
   "byte formatting stays readable at every scale"라는 테스트가 있었는데 정작 1 MB까지만
   검사하고 있었다. GB 단계를 넣고 경계값을 테스트에 추가했다.
3. **`demangle`의 타입 재귀에 깊이 제한이 없었다.** 포인터·한정자가 수천 겹인 심볼에서
   스택 오버플로가 `RangeError`로 튀어, "모르면 null을 돌려준다"는 계약을 깨고 예외가
   밖으로 샜다. 깊이 64로 제한했다(실제 시그니처는 근처도 안 간다).

점검했으나 문제 아니었던 것: `formatBytes(NaN)`이 `"NaN MB"`를 내지만 크기는 전부
`parseInt(hex, 16)`에서 오므로 도달 불가. `splitOrigin`은 경계 입력에서 정상.
`matchParen`은 `#` 주석 안의 괄호를 이미 건너뛴다.

**성능** — README의 개선 작업은 끝나 있고 남은 항목이 없다. 다만 표의 수치
(`loadModel` 2,161 → 265 ms 등)는 **맥에서 타겟 2000개짜리 프로젝트로 잰 것**이고
윈도우에서 재현된 적은 없다. 이번에 추가한 코드가 부담을 더하지 않는 것만 확인했다:
맵 파싱 1.67 ms, 심볼 661개 디맹글 8.85 ms, 타겟 2000개 조인 0.64 ms.

**다음 할 것** — README의 macOS 전용 명령을 윈도우와 병기한다. 네 군데다:
설치의 `ln -s` → `mklink /J`, 통합테스트의 `/Applications/...` 경로, `/tmp/it.log`,
`python3` → `python`(윈도우에서는 스토어 스텁에 걸린다). `test/bootstrap.sh`도 sh
스크립트라 윈도우에서는 `python test/make-fixture.py`를 직접 부르라는 안내가 필요하다.

**보류** — MSVC 맵 포맷(`link.exe /MAP`). 제품 바이너리는 리눅스 GNU 빌드라 이미
지원된다. 윈도우 빌드 용량을 볼 일이 생기면 그때, 실물 샘플부터 확보하고 시작한다.

**막힌 것** — 확장 호스트 35개는 VS Code 실행 경로가 macOS 전용이라 미실행.
`include-test`의 include 해석 24개는 실제 빌드 트리를 요구한다. 윈도우에 C++
컴파일러가 없어 진짜 빌드는 불가. **실제 회사 프로젝트에 붙여본 적이 아직 없다** —
이게 여전히 제일 큰 미확인 항목이다.
