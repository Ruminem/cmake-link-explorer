# NEXT

**여기까지 됨** — 0.1.1을 태그로 릴리스하고, 샘플이 아닌 **진짜 오픈소스
프로젝트(spdlog)에 붙여봤다.**

**릴리스 자동화** — `.github/workflows/release.yml`이 태그↔`package.json` 버전 대조
→ 테스트 → 패키징 → CRLF 검사 → zip → 노트 → 에셋 둘 업로드까지 한번에 돌았다.
`ubuntu-latest`에서 테스트가 전부 통과했다 — 직전 세션의 유일한 미검증 항목이었다.

**진짜 프로젝트 검증** — spdlog를 받아 MinGW g++로 `-Wl,-Map=`을 걸고 빌드해
1.47 MB짜리 진짜 GNU ld 맵을 만들었다. 여섯 기능을 전부 통과시켰고, 그 과정에서
**실행 파일이 맵과 조인되지 않는 버그**를 잡았다. 실행 파일의 `.dir` 표식은
오브젝트 열이 아니라 **아카이브 열에 역슬래시로** 들어 있어서, 둘 다 보도록
고치니 사이즈 귀속이 63% → 87.4%가 됐다. 실제 맵을 그대로 떠 테스트를 넣었다.

이번에도 버그는 전부 **조용했다** — 에러 없이 크기 열만 비어 보였다. 샘플로는
안 드러난다는 것이 다시 확인됐다.

**패키징** — `.gitignore`는 릴리스 zip을 무시하지만 `.vscodeignore`는 아니어서,
로컬에 남아 있던 zip이 다음 VSIX 안으로 통째 들어갔다(75 → 152 KB). `*.vsix`와
`*-windows.zip`을 막았다. CI는 zip을 패키징 뒤에 만들어서 운 좋게 피해 있었을 뿐이다.

**다음 할 것** — 실제 회사 프로젝트. spdlog는 타겟 5개짜리라 순환도 미사용도
나올 것이 없었다. 수백 개 규모에서 트리 정렬과 순환 탐지가 쓸만한지가 다음
세션의 값어치다.

**보류** — 마켓플레이스 게시(Azure DevOps 계정과 토큰만 남음). MSVC / lld 맵 포맷은
실물 샘플이 생기면.

**막힌 것** — 없음. 단위 162 + 확장 호스트 35 + 리눅스 워크플로까지 전부 돌아간다.
