# NEXT

**여기까지 됨** — 후보 3번(순환 의존 / 미사용 타겟)을 넣었다. **152개 통과, 실패 0.**
**Find Cycles and Unused Targets** 명령이 출력 패널에 결과를 쓴다.

만들기 전에 두 가지를 실험으로 확인했고, 둘 다 설계를 바꿨다:

1. **CMake는 정적 라이브러리끼리의 순환을 통과시킨다.** `a→b→c→a`로 configure가
   그냥 됐다. 죽은 코드가 아니라 실재하는 상황이다.
2. **그런데 그 순환이 `dependencies`에는 없다.** `dependencies`는 빌드 순서라
   CMake가 순환을 닫는 간선을 빼고 준다 — `c.dependencies`가 비어서 온다.
   순환은 `linkLibraries`(쓰여진 그대로의 링크 목록)에만 남는다.

그래서 `linkTargetIds`를 모델에 새로 싣고 거기서 Tarjan(명시적 스택, 타겟 수만큼
재귀하지 않도록)으로 찾는다. 구형 코드모델에는 `linkLibraries`가 없는데, 그럴 땐
**"없음"이 아니라 "판단 불가"라고 답한다.** 볼 수 없는 것을 없다고 하면 안 된다.
단 간선이 하나도 없는 프로젝트는 순환이 있을 수 없으므로 "없음"이라고 답한다.
필드 존재로 판단하고 CMake 버전으로 가르지 않는다 — 링크가 없는 타겟은 신형
코드모델에서도 그 필드를 생략하므로, "하나라도 있으면 지원"이 옳은 판정이다.

미사용 판정에서 빼는 것(오탐 방지, 각각 테스트 있음): 실행 파일(진입점),
`install()`된 라이브러리(그게 배포물이다 — 코드모델에 `install.destinations`로 온다),
MODULE 라이브러리(`dlopen`으로 쓴다), UTILITY 타겟. 들어오는 간선은 `linkedBy`와
`linkTargetIds` 양쪽에서 모은다 — 각각이 상대가 가진 것을 놓친다.

`dependencySites`도 `linkLibraries` 우선으로 바꿨다. 그래야 순환을 닫는 간선의
`파일:줄`이 나와서 출력 패널에서 클릭된다.

**다음 할 것** — 후보 4번: **윈도우 트리 vs 리눅스 트리 비교.** 두 코드모델에서
타겟·매크로·include가 어긋난 곳을 짚어 "윈도우에선 되는데 리눅스에서 깨지는" 부류를
빌드 전에 잡는다. 2번에서 넣은 `compileGroups`가 그대로 재료가 된다. 두 트리가 같은
머신에 있어야 하고, 앞의 셋보다 크다. README 로드맵에도 올려뒀다.

**그다음** — README의 macOS 전용 명령 병기(`ln -s` → `mklink /J`, `/Applications/...`,
`/tmp/...`, `python3` → `python`).

**보류** — MSVC 맵 포맷. 제품 바이너리는 리눅스 GNU라 이미 지원된다. 필요해지면
실물 샘플부터. 그래프 뷰(웹뷰)는 지금 트리가 답하는 질문을 더 예쁘게 답할 뿐이라
우선순위 낮음.

**막힌 것** — 확장 호스트 35개는 VS Code 실행 경로가 macOS 전용이라 미실행.
새로 넣은 `compileSettings`/`projectHealth` 명령도 확장 호스트 테스트가 없다
(로직은 단위 테스트로 덮음). 윈도우에 C++ 컴파일러가 없어 진짜 빌드는 불가.
**실제 회사 프로젝트에 붙여본 적이 아직 없다** — 여전히 제일 큰 미확인 항목.
