# NEXT

**여기까지 됨** — 맥북에서 성능 수정(전이 축약 비트맵화, CMakeLists 파싱 O(n log n),
디맹글 지연 처리)과 정확성 버그 4건을 `6e181e3`로 커밋. 윈도우 `C:\dev\cmake-link-explorer`에
클론해서 양쪽 기기 준비 완료.

**다음 할 것** — 윈도우에서 테스트 4종을 돌려 경로 대소문자 수정이 실제 윈도우에서
동작하는지 확인. 이 수정은 윈도우를 위해 한 것인데 아직 윈도우에서 검증된 적이 없다.

**막힌 것** — 윈도우에 `node`가 없어 테스트를 못 돌린다(`cmake`/`ninja`도 없지만
그건 sample-project 빌드에만 필요). README의 설치·통합테스트 명령이 macOS 전용이라
윈도우 절차 병기가 필요하다.
