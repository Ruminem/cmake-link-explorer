# NEXT

**여기까지 됨** — 0.1.0을 릴리스한 뒤 배포와 성능을 정리했다.

**배포** — Windows용 더블클릭 번들(`install.cmd` + VSIX를 담은 zip)을 만들었다.
스크립트가 `Code.exe`를 직접 찾으므로 **`code`가 PATH에 없어도 된다** — VS Code를 갓
깔면 터미널이 옛 PATH를 들고 있어 실제로 걸리는 문제다. 압축을 풀어 직접 실행해
확인했다. `.gitattributes`로 CRLF를 고정했고(LF면 `goto`가 깨진다), `.vscodeignore`로
VSIX에서 제외했다.

**릴리스 자동화** — `.github/workflows/release.yml`. 태그를 밀면 태그↔`package.json`
버전 대조 → 테스트 → 패키징 → CRLF 검사 → zip → 노트 → 두 에셋 업로드까지 한다.
0.1.0은 손으로 만들어 올렸다.

**성능** — 타겟 2,000개·간선 209,199개를 합성해 재보니 오늘 추가한 필드가
`loadModel`을 **+24%(224→278ms)** 만들고 있었다. 원인은 `dependencySites`가 폐포
전체에 `backtraceGraph`를 다시 걸던 것. 백트레이스 인덱스로 메모이즈(+54→+42ms)하고,
축약 이후 **직접 간선과 `linkTargetIds`만 남기도록** 걸러냈다(209,199 → 10,563 항목,
모델 힙 22.7 → 14.6 MB). 잘못 걸러내면 "Why Is This Linked?"의 줄 번호가 조용히
사라지므로 양방향을 검사하는 테스트를 넣었다.

**아이콘** — 사슬 고리를 가운데서 가른 모양. 색은 트리가 쓰는 것과 같다
(`charts.orange` = 얘를 링크하는 것, `charts.blue` = 얘가 링크하는 것). 32px에서도
안 뭉개지는지 실제로 그려 보고 골랐다. 갤러리용 PNG와 활동 표시줄용 단색 SVG.

**문서** — 영어가 기본(`README.md`), 한국어는 `README.ko.md`이고 음슴체.
mermaid 다이어그램 4개. 최상단 기능 표가 각 절로 링크된다.

**다음 할 것** — **실제 회사 프로젝트에 붙여보기.** 설치가 한 줄이 됐고 확장 호스트
35개도 통과하니 걸림돌이 없다. 이번 세션에 잡은 버그가 전부 "실제로 돌려보니 드러난"
것들이었다. 샘플이 아닌 진짜 프로젝트에서 무엇이 나오는지가 다음 세션의 값어치다.

**보류** — 마켓플레이스 게시. 퍼블리셔 ID와 PNG 아이콘은 갖춰졌고 Azure DevOps 계정과
토큰만 남았다. MSVC / lld 맵 포맷은 실물 샘플이 생기면.

**막힌 것** — 없음. 윈도우에서 돌릴 수 있는 검증은 전부 돌아간다(단위 161 + 확장
호스트 35). 릴리스 워크플로의 테스트 단계는 `ubuntu-latest`에서 아직 안 돌아봤다 —
첫 태그가 그 검증이다.
