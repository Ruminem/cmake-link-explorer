# NEXT

**여기까지 됨** — 실제 워크플로가 확정됐다: **평소 개발은 윈도우 + MSVC, 제품판 빌드는
리눅스 + GNU, 확장은 윈도우 VS Code에서 사용.** 즉 맵과 빌드 트리가 다른 OS에서
나오는 게 정상 시나리오다. `matchTargets`가 `nameOnDisk`를 정확히 키로 삼아 이 조합에서
아무것도 매칭하지 못하던 것을 고쳤다(에러 없이 크기 열만 비어 보이던 문제).
정확 일치를 먼저 시도하고 **실패했을 때만** `lib` 접두사와 확장자를 벗긴 어간으로
비교한다. 윈도우 MSVC 트리 + 리눅스 GNU 맵이 0개 → 1개 매칭으로 바뀌었고,
리눅스 트리 결과는 그대로다. 크로스 OS 조인 테스트 7개 추가, **94개 통과 실패 0**.

안전장치: 어간이 겹치는 타겟이 둘 이상이면 추측하지 않고 버린다. 정확 일치가 항상
어간을 이긴다. 오브젝트 파일(`app.o`)은 어간 매칭 대상에서 제외한다 — 그건 `.dir`
규칙이 할 일이고, 이름만으로 흡수하면 엉뚱한 타겟에 붙는다. 각각 테스트가 있다.

**다음 할 것** — MSVC 맵 포맷. 윈도우에서 뽑은 맵(`link.exe /MAP`)은 `detectFormat`이
거절한다. 파서 신규 작성은 주말 예산을 통째로 먹으니, **먼저 실물 샘플부터 확보한다.**
회사 프로젝트를 MSVC로 빌드할 때 `/MAP` 옵션을 주면 나온다. 샘플을 `test/maps/`에
커밋해두면 그다음 주말에 파서를 붙일 수 있다. 실물 없이 포맷을 추측해 넣지 않는다는
원칙은 유지(lld를 뺀 것과 같은 이유).

**그다음** — `demanglerCommand` 기본값 `c++filt`가 윈도우에 없다. 리눅스에서 만든 맵의
GCC 심볼을 윈도우에서 풀어야 하므로 실제로 걸린다. 기본값 탐색을 넓히거나 README에
설정법을 적는다.

**막힌 것** — 확장 호스트 35개는 VS Code 실행 경로가 macOS 전용이라 미실행.
`include-test`는 28개가 실제 빌드 트리를 요구한다(합성 픽스처로 풀리는지 미검증).
윈도우에 C++ 컴파일러가 없어 진짜 빌드는 불가. README의 설치·통합테스트 명령도
macOS 전용이다(`ln -s`, `/Applications/...`, `/tmp/...`, `python3` → `python`).
