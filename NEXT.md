# NEXT

**여기까지 됨** — 윈도우에서 전체 스위트를 처음 완주했다(Node 24.19.0, CMake 4.4.3).
**133개 시도 / 131개 통과.** 목표였던 경로 대소문자 수정이 검증됐다: `include-test`
30/30, 실제 CMake 트리 대상 `run.js` 18/18. 컴파일러가 없어 configure가 막혔는데,
테스트는 빌드를 안 하고 File API만 읽으므로 `-DCMAKE_CXX_COMPILER_WORKS=1`과
컴파일러 ID 강제로 우회했다(빌드 트리는 gitignore 대상이라 커밋 안 됨).

**다음 할 것** — 맵↔타겟 조인 테스트를 이식 가능하게 고친다. 실패 2개는 제품 버그가
아니라 테스트가 **macOS에서만 통과하도록** 되어 있어서다: 커밋된 `sample-app.map`은
ld64 산출물이라 `librender_core.dylib`를 담는데, 로컬 configure는 플랫폼마다
`.dll`/`.so`를 낸다. 실제 MSVC를 깔아도 똑같이 실패한다. 공유 라이브러리 매칭을
확장자 무관으로 하거나, 조인 테스트를 맥 전용으로 명시해 건너뛰게 해야 한다.

**막힌 것** — 윈도우에 C++ 컴파일러가 없어 진짜 빌드는 못 한다(위 우회로 configure만
됨). `c++filt`도 없어서 `demanglerCommand` 기본값이 윈도우에서 무용지물이다 —
코드는 안전하게 처리하지만 실사용에선 디맹글링이 그냥 안 되니 대안이 필요하다.
확장 호스트 35개도 미실행이고, README의 설치·통합테스트 명령은 여전히 macOS 전용이다
(`ln -s`, `/Applications/...`, `/tmp/...`, 그리고 `python3`는 윈도우에서 스토어 스텁에
걸리므로 `python`).
